<?xml version="1.0" encoding="US-ASCII"?>
<testcase>
<info>
<keywords>
HTTP
HTTP GET
HTTP Negotiate auth (stub ntlm)
SPNEGO NTLM disallowed
</keywords>
</info>

# Server-side
<reply>
<data nocheck="yes" crlf="headers">
HTTP/1.1 200 OK swsclose
Content-Length: 23

This IS the real page!
</data>
</reply>

# Client-side
<client>
<server>
http
</server>
<name>
SPNEGO rejects NTLM when --no-spnego-ntlm-allowed is set
</name>
<features>
GSS-API
Debug
</features>
<setenv>
CURL_STUB_GSS_CREDS="NTLM_Alice"
</setenv>
<command>
--negotiate --no-spnego-ntlm-allowed --spnego-fail-on-error http://%HOSTIP:%HTTPPORT/%TESTNUMBER
</command>
</client>

# Verify data after the test has been "shot"
<verify>
# CURLE_AUTH_ERROR (94)
<errorcode>
94
</errorcode>
</verify>
</testcase>
