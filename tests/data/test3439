<testcase>
<info>
<keywords>
SIEVE
GETSCRIPT
</keywords>
</info>

#
# Server-side
<reply>
<servercmd>
REPLY GETSCRIPT \"# This is an empty one-liner script."\r\nOK
</servercmd>
</reply>

#
# Client-side
<client>
<server>
sieve
</server>
 <name>
SIEVE GETSCRIPT with returned data in a quoted string
 </name>
 <command>
sieve://%HOSTIP:%SIEVEPORT/script%TESTNUMBER
</command>
</client>

#
# Verify data after the test has been "shot"
<verify>
<protocol>
GETSCRIPT "script%TESTNUMBER"
LOGOUT
</protocol>
<stdout nonewline="yes">
# This is an empty one-liner script.
</stdout>
</verify>
</testcase>
