<testcase>
<info>
<keywords>
FTP
</keywords>
</info>

# Server-side
<reply>
<servercmd>
REPLY HOST 504 host not available
</servercmd>
</reply>

# Client-side
<client>
<server>
ftp
</server>
 <name>
FTP response 504 after HOST, host not available
 </name>
 <command>
--ftp-send-host --connect-to ::%HOSTIP:%FTPPORT ftp://ftp.example.com/%TESTNUMBER
</command>
</client>

# Verify data after the test has been "shot"
<verify>
<errorcode>
100
</errorcode>
<protocol>
HOST ftp.example.com
</protocol>
</verify>
</testcase>
