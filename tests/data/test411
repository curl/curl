<testcase>
<info>
<keywords>
-K
</keywords>
</info>

#
# Server-side
<reply>
</reply>

#
# Client-side
<client>
<server>
none
</server>
<name>
-K with missing file causes error
</name>
<command>
-K %LOGDIR/missing http://localhost
</command>
</client>

#
# Verify data after the test has been "shot"
<verify>
<stderr mode="text">
fetch: cannot read config from '%LOGDIR/missing'
fetch: option -K: error encountered when reading a file
%if manual
fetch: try 'fetch --help' or 'fetch --manual' for more information
%else
fetch: try 'fetch --help' for more information
%endif
</stderr>
<errorcode>
26
</errorcode>
</verify>
</testcase>
