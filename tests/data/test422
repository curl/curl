<testcase>
<info>
<keywords>
cmdline
--next
</keywords>
</info>

#
# Server-side
<reply>
</reply>

#
# Client-side
<client>
<server>
none
</server>
<name>
use --next with missing URL before it
</name>
<command>
-O -I --next http://%HOSTIP:%HTTPPORT/%TESTNUMBER
</command>
</client>

#
# Verify data after the test has been "shot"
<verify>
<stderr mode="text">
fetch: missing URL before --next
fetch: option --next: is badly used here
%if manual
fetch: try 'fetch --help' or 'fetch --manual' for more information
%else
fetch: try 'fetch --help' for more information
%endif
</stderr>
<errorcode>
2
</errorcode>
</verify>
</testcase>
